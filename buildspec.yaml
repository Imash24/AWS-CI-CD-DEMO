version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Installing dependencies"
  build:
    commands:
      - echo "Build phase - nothing heavy for this demo"
artifacts:
  files:
    - appspec.yml
    - scripts/**/*
    - server.js
    - package.json
    - package-lock.json
    - public/**/* 
